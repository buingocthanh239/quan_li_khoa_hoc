<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container"> 
      <a class="navbar-brand" href="/"> FreeCourse </a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown">
          <a
            class="nav-link dropdown-toggle"
            href="#"
            id="navbarDropdown"
            role="button"
            data-toggle="dropdown"
            aria-haspopup="true"
            aria-expanded="false"
          >
           {{user.name}}
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="/courses/store/{{user.email}}">Khóa học đã lưu</a>
            <div class="dropdown-divider"></div>
            {{#if (edit user.role)}}
              <a class="dropdown-item" href="/me/stored/courses">Khóa học của tôi </a>
              <a class="dropdown-item" href="/courses/create">Đăng khóa học</a>
              <div class="dropdown-divider"></div>
            {{/if}}
            {{#if (admin user.role)}}
              <a class="dropdown-item" href="/admin/get-all-users">quản lí người dùng </a>
            <div class="dropdown-divider"></div>
            {{/if}}
            <a class="dropdown-item" href="/user/logout">Đăng xuất</a>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>